import { getAssignmentFolder, spawnProcess } from './utils.js';

/** Downloads the latest assigment tests */
export default async function test() {
  const name = '@plugga-tech/' + (await getAssignmentFolder()).name;
  await spawnProcess('npm', ['update', name]);
  await spawnProcess('npx', ['plugga', 'init']);
}
